---
  - name: "Playing with Ansible and Git"
    hosts: all
    become: true
    become_user: 
    tasks:
    - name: test
      yum:
        name: git
        state: present
      shell: |
         cd ~
         mkdir sreekanth
