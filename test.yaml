---
  - name: "Playing with Ansible and Git"
    hosts: all
    become: true
    become_user: 
    tasks:
    - name: Package Install
      yum:
        name: git
        state: present
    - name: Folder Creation
      shell: |
         cd ~
         mkdir sreekanth
